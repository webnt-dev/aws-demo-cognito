{
  "name": "cognito4signuppostconfirmation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "all": "eslint --fix -c .eslintrc ./src/**/*.mts && tsc",
    "lint": "eslint --fix -c .eslintrc  ./src/**/*.mts  ",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.338.0"
  }
}
